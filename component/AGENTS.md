
#### 動画ホバー時プレビュー
- フレーム切替間隔: **350ms**
- ホバー解除で元サムネイルに復帰
- OpenCV 使用

---

### 5. メタ情報表示

#### 共通
- ファイルサイズ表示
  - B / KB / MB / GB 自動切替

#### 動画
- OpenCV で取得
  - FPS
  - 総フレーム数
  - 再生時間算出
  - 解像度

#### 画像
- Pillow により解像度取得

---

### 6. 操作 (Actions)

- **サムネクリック**
  - 詳細表示用コールバック呼び出し

- **フォルダ**
  - OS依存で実装
    - Windows: Explorer
    - Linux: xdg-open
    - macOS: open
  - 該当ファイルを開く or 選択

- **削除**
  - 確認ダイアログ表示
  - 削除コールバック実行
  - UIからカードを除去
  - グループ内が空になった場合、グループ自体を非表示

---

### 7. 外部注入可能な要素

- サムネイルキャッシュ
- 遅延ロードキュー
- ウィジェットマップ
- スタイル指定
  - カード背景
  - 枠線
  - 角丸
  - ホバー枠色

---

## 性能・主要パラメータ (Current Parameters)

| 項目 | 値 |
|----|----|
| プレビュー更新間隔 | 350ms |
| 最大列数 | 4 |
| サムネ最大サイズ | 288 × 162 |
| 情報パネル幅 | 256px |
| カード最小高さ | サムネ高さ + 84px |

---

## 重複判定ロジックに関する前提

⚠ **本UIファイル単体では、重複判定のロジックは定義されていない。**

判定精度・挙動は別モジュールに委ねられており、
UI側は **「すでにグルーピングされた結果を表示する」責務のみを持つ**。

---

## 今後の検討事項 (Guidelines for Future Agents)

### 精度向上
- ハッシュ方式の併用
  - 全体ハッシュ + 部分ハッシュ
  - aHash / dHash / pHash
- 感度可変・閾値調整UI
- 動画
  - キーフレーム抽出
  - フレーム類似度判定

---

### 調整可能パラメータ候補
- 類似度スコアのしきい値
- 比較フレーム数 / フレーム間隔
- プレビュー更新間隔（現状 350ms）
- 最大列数（現状 4）
- サムネ解像度（現状 最大 288×162）

---

### UX / 性能改善
- サムネ・プレビューフレームの遅延ロード + キャッシュ強化
- リサイズ時のレイアウト再計算デバウンス
- OpenCV / Pillow の例外ハンドリング強化
- ファイル欠損時もプレースホルダーを維持

---

### メモリ / I/O
- 動画プレビューフレームは最小限のみ保持
- 長尺動画は間引き処理
- VideoCapture の `release()` 漏れ防止（現状あり）

---

### 観測・可視化
- 処理時間ログ
- ヒット率 / 誤検出率の計測
- 閾値調整の根拠を残すためのメトリクス化

---

## エージェントへの指針

- UIコードでは **重複判定ロジックを仮定しない**
- パフォーマンス退行を起こす変更は必ず計測を伴う
- ファイル削除は常に **安全側（確認あり）** に倒す
- 見た目変更は **既存パラメータを尊重** すること

---

以上を前提として作業すること。
